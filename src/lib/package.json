{
  "name": "@rayelder/context",
  "version": "0.0.9",
  "description": "A design system component library",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc && copyfiles \"components/**/*.module.css\" dist/ --up 0",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "react-dom": ">=18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "copyfiles": "^2.4.1",
    "typescript": "^5.0.0"
  }
}
